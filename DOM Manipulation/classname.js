// This will remove the black class from the div (id="first") From style
first.classList.remove('black')   

// This will add the red class from the div (id="first") From style

first.classList.add('red')

// This will remove the red class from the div (id="first") From style
 first.classList.remove("red")

 // This will add the blue class from the div (id="first") From style
first.classList.add("blue")

// This will check if the element is contain the class name (red or blue) or not...
first.classList.contains('blue')
first.classList.contains('red')


